apiVersion: v1
kind: ConfigMap
metadata:
  name: rate-limiter-config
  namespace: fintech-staging
data:
  maxRequestsPerMinute: "10"          # safe baseline value
  burstLimit: "150"
  windowSeconds: "60"
  enabled: "true"
